import React from 'react';
// import PropTypes from 'prop-types';

export const className = 'questionManager';

const QuestionManager = () => (<div className={className} />);

QuestionManager.propTypes = {};

export default QuestionManager;
